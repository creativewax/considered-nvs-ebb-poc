// src/components/layout/DesktopKeyline.jsx

// Cosmetic frame border — visible on desktop only via CSS.
// The actual border is drawn in global.css via #root::before pseudo-element,
// so this component intentionally renders nothing visible itself.
export default function DesktopKeyline() {
  return null
}
